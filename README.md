# AUTOMATIC-NUMBER-PLATE-RECOGNITION-SYSTEM
This code uploads a video, processes each frame using EasyOCR to detect and recognize text (like vehicle license plates), and draws bounding boxes with recognized text on the frames. The processed frames are saved into a new output video file. Finally, the code allows downloading the annotated video from Google Colab.
